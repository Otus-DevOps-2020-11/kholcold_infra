---
- name: Install ruby
  hosts: all
  become: true
  tasks:
    - name: Install package ruby
      apt:
        name:
          - ruby-full
          - ruby-bundler
          - build-essential
        state: present
        update_cache: yes
